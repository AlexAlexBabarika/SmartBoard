# AI Investment Scout DAO - Environment Variables Template
# Copy this file's contents to create your .env file

# ===========================================
# LLM Provider Configuration
# ===========================================
# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Choose your LLM provider: openai, anthropic, or local
LLM_PROVIDER=openai

# Model to use (gpt-4, gpt-3.5-turbo, claude-3-opus-20240229, etc.)
LLM_MODEL=gpt-4

# ===========================================
# IPFS Storage Configuration (Storacha - no API keys)
# ===========================================
# Install Storacha CLI and log in:
#   npm i -g @storacha/cli
#   storacha login
# Select or create a space for uploads:
#   storacha space create dao
#   storacha space use dao
# Optional: override CLI binary name
STORACHA_CLI=storacha

# ===========================================
# NEO Blockchain Configuration
# ===========================================
# NEO testnet RPC endpoint
NEO_RPC_URL=https://testnet1.neo.org:443

# Your NEO testnet wallet private key (NEVER USE MAINNET KEYS!)
# Get testnet GAS from: https://neowish.ngd.network/
NEO_WALLET_PRIVATE_KEY=your-private-key-here-for-testnet-only

# Deployed contract hash (set this after deploying the contract)
NEO_CONTRACT_HASH=0x1234567890abcdef

# ===========================================
# Backend Configuration
# ===========================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Database URL (SQLite for dev, PostgreSQL for production)
DATABASE_URL=sqlite:///./proposals.db
# For PostgreSQL: postgresql://user:password@localhost/dbname

# ===========================================
# SpoonOS Configuration
# ===========================================
# Path to SpoonOS config file (if using spoon-cli)
SPOON_CLI_CONFIG=./spoon_agent/spoon.config.json

# ===========================================
# Application Mode
# ===========================================
# Set to 'true' to simulate blockchain and IPFS operations
# Set to 'false' to use real APIs (requires all keys above)
DEMO_MODE=true

# ===========================================
# Automatic Startup Discovery
# ===========================================
# Enable automatic startup discovery and processing
# Set to 'true' to automatically search for startups periodically
AUTO_SEARCH_STARTUPS=false

# How often to search for startups (in hours)
# Default: 24 (once per day)
SEARCH_INTERVAL_HOURS=24

# Sources to search for startups (comma-separated)
# Options: demo, producthunt, crunchbase, ycombinator
# Default: demo,producthunt
# Note: Set DEMO_MODE=false and provide API keys for real data
STARTUP_SEARCH_SOURCES=demo,producthunt

# Optional: Product Hunt API token (for better rate limits)
# Get from: https://api.producthunt.com/v2/oauth/applications
# PRODUCT_HUNT_API_TOKEN=your-product-hunt-token-here

# Optional: Crunchbase API key (for real Crunchbase integration)
# Get from: https://data.crunchbase.com/
# CRUNCHBASE_API_KEY=your-crunchbase-api-key-here

# ===========================================
# Optional: Anthropic Configuration
# ===========================================
# If using Anthropic instead of OpenAI
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# ===========================================
# Optional: Logging Configuration
# ===========================================
# LOG_LEVEL=INFO
# LOG_FILE=app.log

# ===========================================
# Optional: Email Configuration
# ===========================================
# SMTP server for sending notification emails
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=your-email@gmail.com
# EMAIL_FROM_NAME=SmartBoard Team
#
# Note: For Gmail, you'll need to use an App Password:
# 1. Go to Google Account settings
# 2. Enable 2-Step Verification
# 3. Generate an App Password for "Mail"
# 4. Use that App Password as SMTP_PASSWORD

# ===========================================
# Optional: Blockchain Event Listener
# ===========================================
# How often to poll the blockchain for finalized proposals (in seconds)
# Default: 30 (checks every 30 seconds)
# BLOCKCHAIN_POLL_INTERVAL=30
#
# Note: In production mode (DEMO_MODE=false), the blockchain listener
# automatically monitors the smart contract for proposal finalization events
# and triggers email notifications. In demo mode, emails are sent via API endpoints.

# ===========================================
# Optional: Security Configuration
# ===========================================
# JWT_SECRET=your-secret-key-for-jwt
# API_KEY=your-api-key-for-external-access

# ===========================================
# NOTES:
# ===========================================
# 1. NEVER commit this file with real credentials to version control
# 2. Use testnet keys only, never mainnet
# 3. For production, use a secrets manager (AWS Secrets Manager, HashiCorp Vault)
# 4. Demo mode works without any API keys (simulated operations)
# 5. Each developer should have their own .env file
# 6. Keep a backup of your keys in a secure location

